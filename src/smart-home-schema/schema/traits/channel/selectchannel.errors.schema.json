{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "An error occurred selecting a new channel on the device.",
  "type": "string",
  "enum": [
    "noAvailableChannel",
    "noChannelSubscription",
    "channelSwitchFailed"
  ]
}
