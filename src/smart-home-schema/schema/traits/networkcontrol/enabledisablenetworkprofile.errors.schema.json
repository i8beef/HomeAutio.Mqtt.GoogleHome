{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "An error occurred while attempting to control the given network profile.",
  "type": "string",
  "enum": [
    "networkProfileNotRecognized"
  ]
}
