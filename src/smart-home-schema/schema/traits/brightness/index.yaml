name: action.devices.traits.Brightness
shortname: Brightness
description: This trait covers how to control the brightness of a device.
availability: production
version: '1.0'
attributes:
  $ref: brightness.attributes.schema.json
states:
  $ref: brightness.states.schema.json
commands:
  action.devices.commands.BrightnessAbsolute:
    shortname: BrightnessAbsolute
    params:
      $ref: brightnessabsolute.params.schema.json
  action.devices.commands.BrightnessRelative:
    shortname: BrightnessRelative
    params:
      $ref: brightnessrelative.params.schema.json
