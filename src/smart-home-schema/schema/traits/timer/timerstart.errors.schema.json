{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "An error occurred starting the timer.",
  "type": "string",
  "enum": [
    "noTimerExists",
    "timerValueOutOfRange",
    "aboveMaximumTimerDuration",
    "belowMinimumTimerDuration"
  ]
}
