{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "examples": [
    {
      "$comment": "No parameters"
    }
  ],
  "description": "Select the next input. Only applicable when the <code>orderedInputs</code> attribute is set to true.",
  "type": "object",
  "additionalProperties": false
}
