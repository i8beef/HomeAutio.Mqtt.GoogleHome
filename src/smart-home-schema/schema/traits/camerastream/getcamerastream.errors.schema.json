{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "An error occurred while attempting to retrieve the stream.",
  "type": "string",
  "enum": [
    "resourceUnavailable"
  ]
}
