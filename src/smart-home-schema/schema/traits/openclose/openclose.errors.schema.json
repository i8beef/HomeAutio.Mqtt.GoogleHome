{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "An error occurred setting the open-close state of the device.",
  "type": "string",
  "enum": [
    "lockedState",
    "deviceJammingDetected"
  ]
}
