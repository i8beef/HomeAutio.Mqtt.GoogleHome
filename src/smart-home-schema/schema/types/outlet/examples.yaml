name: Simple outlet
type: action.devices.types.OUTLET
traits:
  - action.devices.traits.OnOff
states:
  {
    "on": true
  }
commands:
  action.devices.commands.OnOff:
    params:
      {
        "on": true
      }
    results:
      {
        "on": true
      }
