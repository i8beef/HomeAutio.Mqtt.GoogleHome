name: Simple switch
type: action.devices.types.SWITCH
traits:
  - action.devices.traits.OnOff
states:
  {
    "on": true
  }
commands:
  action.devices.commands.OnOff:
    params:
      {
        "on": true
      }
    results:
      {
        "on": true
      }
