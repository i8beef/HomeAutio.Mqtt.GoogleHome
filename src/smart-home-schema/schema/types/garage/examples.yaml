name: Simple garage
type: action.devices.types.GARAGE
traits:
  - action.devices.traits.OpenClose
states:
  {
    "openPercent": 50
  }
commands:
  action.devices.commands.OpenClose:
    params:
      {
        "openPercent": 100
      }
    results:
      {
        "openPercent": 100
      }
