name: action.devices.types.SETTOP
shortname: Set-top Box
description: >
  Interactions with Multichannel Video Programming Distributor (MVPD) and set-top-box
  devices
  may include controlling media playback.
availability: production
version: '1.0'
traits:
  recommended:
  - Volume
  required:
  - AppSelector
  - MediaState
  - Channel
  - OnOff
  - TransportControl
