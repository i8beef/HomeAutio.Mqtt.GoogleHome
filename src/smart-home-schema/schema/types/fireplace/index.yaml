name: action.devices.types.FIREPLACE
shortname: Fireplace
description: >
  Fireplaces can be turned on and off, and may have adjustable modes.
availability: production
version: '1.0'
traits:
  recommended:
  # Options such as discrete heat levels
  - Modes
  # Controls such as opening a vent
  - Toggles
  - OnOff
  required: []
