name: Simple valve
type: action.devices.types.VALVE
traits:
  - action.devices.traits.OpenClose
states:
  {
    "openPercent": 100
  }
commands:
  action.devices.commands.OpenClose:
    params:
      {
        "openPercent": 0
      }
    results:
      {
        "openPercent": 0
      }
